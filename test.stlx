load("diff.stlx");

evalStr([
	"x + 0",
	"x * 0",
	"x + 0 * b - 6 - 3",
	"(x + 7) * 34",
	"diff(a, a)",
	"diff(diff(x * x * y, x), y)",
	"diff(f / g / h, f)",
	"diff(x * x, x)",
	"(x * (7 + 8 / x)) - diff(7 * x * x * x, x)",
	"diff(diff(cos(sin(x)), x), x)",
	"-6 * (-3) * x",
	"diff(sin(x) * x^2, x) * cos(x)",
	"a^(x^y*b)"
]);